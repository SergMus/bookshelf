import { TdContentDirective } from './td-content.directive';

describe('TdContentDirective', () => {
  it('should create an instance', () => {
    const directive = new TdContentDirective();
    expect(directive).toBeTruthy();
  });
});
